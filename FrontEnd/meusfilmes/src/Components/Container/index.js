import React from "react";
import {CompContainer} from "./styled"

export default function Container ({children}) {
    return(
        <CompContainer>
            {children}
        </CompContainer>
    )
}